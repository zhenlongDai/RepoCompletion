#self.model_path = args.model_path
# self.dp_size = args.dp_size
# self.tp_size = args.tp_size
# self.dp_master_ip = "127.0.0.1"
# self.dp_master_port = args.dp_master_port #get_open_port()
# data_path_dir = args.data_path_dir 
# self.debug_mode = args.debug_mode
# self.language = args.language
# self.max_input_tokens = args.max_input_tokens
# self.save_file_path = args.save_file_path
# self.system_prompt = args.system_prompt
# self.temperature = args.temperature
# self.top_p = args.top_p
# self.gpu_memory_utilization = args.gpu_memory_utilization
model_path: "CodeLLM/Qwen2.5-Coder-0.5B-Instruct"
eval_dataset_name: "repobench"
dp_size: 1
tp_size: 1
dp_master_ip: "127.0.0.1"
dp_master_port: 29500
data_path_dir: "datasets/repobench/java/test"
debug_mode: false
language: "java"
max_input_tokens: 512
save_file_path: "output_dir/generation/QwenCoder0.5B.json"
#system_prompt: " You first think about the reasoning process as an internal monologue and then provide the user with the answer. Respond in the following format: <think>\n...\n</think>\n<answer>\n...\n</answer>"
system_prompt: "You are a helpful assistant tasked with completing the next line of a repository-level code file.  You are provided with an incomplete code file, along with other code snippets from the same repository.  You should provide the user with the next line of the incomplete code. Please respond in the following format: <answer>\n```...```\n</answer>"
temperature: 0.7
top_p: 1.0
gpu_memory_utilization: 0.25
dtype: "float16"
     